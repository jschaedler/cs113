public class Practice2C {

    /*
     * public int topPlayers(Player[] team, int target) { int count = 0; for (int i
     * = 0; i < team.length; i++) { if (team[i].getAverage > target) count++; } }
     * 
     * 
     */
}